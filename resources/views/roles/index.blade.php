<!DOCTYPE html><html lang="en"><head><meta charset="UTF-8"><meta name="viewport" content="width=device-width,initial-scale=1"><title>Roles & Permissions</title><link rel="stylesheet" href="{{ asset('vendors/css/vendor.bundle.base.css') }}"><link rel="stylesheet" href="{{ asset('css/vertical-layout-light/style.css') }}"><link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/4.7.0/css/font-awesome.min.css"><style>.role-card{border:0;border-radius:14px;box-shadow:0 6px 22px rgba(15,23,42,.07)}.permission-grid{display:grid;grid-template-columns:repeat(auto-fit,minmax(210px,1fr));gap:12px}.permission-group{background:#f8fafc;border-radius:10px;padding:12px}.permission-group label{display:block;font-size:12px;margin:5px 0}.protected{background:#dcfce7;color:#166534;padding:4px 9px;border-radius:15px;font-size:12px}</style></head><body><div class="container-scroller">@include('include.header')<div class="container-fluid page-body-wrapper">@include('include.sidebar')<div class="main-panel"><div class="content-wrapper"><div class="d-flex justify-content-between align-items-center mb-4"><div><h3>Roles & Permissions</h3><p class="text-muted mb-0">Create roles for {{ $tenant->name }}. The protected Admin account is not counted as a normal user role.</p></div><button class="btn btn-primary" data-toggle="collapse" data-target="#createRole">Create role</button></div>
@if(session('success'))<div class="alert alert-success">{{ session('success') }}</div>@endif @if($errors->any())<div class="alert alert-danger">{{ $errors->first() }}</div>@endif
<div class="collapse mb-4" id="createRole"><div class="card role-card"><div class="card-body"><form method="post" action="{{ route('roles.store') }}">@csrf<div class="form-group"><label>Role name</label><input class="form-control" name="name" placeholder="Example: Agent, Sales Manager, Marketing" required></div><div class="permission-grid">@foreach($permissions as $module=>$items)<div class="permission-group"><strong>{{ ucfirst($module) }}</strong>@foreach($items as $permission)<label><input type="checkbox" name="permissions[]" value="{{ $permission->name }}"> {{ str_replace('-', ' ', ucfirst(str($permission->name)->after('.')->value())) }}</label>@endforeach</div>@endforeach</div><button class="btn btn-primary mt-3">Create role</button></form></div></div></div>
<div class="row">@foreach($roles as $role)<div class="col-lg-6 mb-4"><div class="card role-card h-100"><div class="card-body"><div class="d-flex justify-content-between"><h5>{{ $role->name }}</h5><div><span class="badge badge-light">{{ $role->users_count }} users</span>@if($role->name==='Admin') <span class="protected">Protected</span>@endif</div></div>
@if($role->name==='Admin')<p class="text-muted">The single company Admin always has all tenant permissions and cannot be renamed or deleted.</p>@else<form method="post" action="{{ route('roles.update',$role) }}">@csrf @method('PUT')<input class="form-control mb-3" name="name" value="{{ $role->name }}" required><div class="permission-grid">@foreach($permissions as $module=>$items)<div class="permission-group"><strong>{{ ucfirst($module) }}</strong>@foreach($items as $permission)<label><input type="checkbox" name="permissions[]" value="{{ $permission->name }}" @checked($role->hasPermissionTo($permission))> {{ str_replace('-', ' ', ucfirst(str($permission->name)->after('.')->value())) }}</label>@endforeach</div>@endforeach</div><button class="btn btn-outline-primary btn-sm mt-3">Save role</button></form><form method="post" action="{{ route('roles.destroy',$role) }}" class="mt-2" onsubmit="return confirm('Delete this unused role?')">@csrf @method('DELETE')<button class="btn btn-link text-danger p-0">Delete role</button></form>@endif
</div></div></div>@endforeach</div></div>@include('include.footer')</div></div></div><script src="{{ asset('vendors/js/vendor.bundle.base.js') }}"></script></body></html>
